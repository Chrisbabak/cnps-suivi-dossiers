# Configuration de déploiement Netlify.
[build]
  command = "npm run build"
  publish = "dist"

# Redirection SPA : toutes les routes sont servies par index.html
# (le routage est géré côté client par React Router).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
